print(message: "Bow to the mighty Hydra!\n")

for i in [0, 1, 3] {
    var j = 2 * i

    for k in [0, 1, j] {
        var l = 3 * k
        print(message: "i = \(i), j = \(j), k = \(k), l = \(l)\n")
    }
}

print(message: "Done!\n")